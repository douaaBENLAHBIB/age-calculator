# Age Calculator

A simple Java program that calculates a person's age based on their birth date.

## How it works
The user enters their birth date, and the program calculates their current age.

## How to run
Compile and run `Main.java` using any Java IDE (like IntelliJ) or via command line with `javac` and `java`.
